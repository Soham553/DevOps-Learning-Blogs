Hello everyone, my name is Soham I am going to share my linux with you.

Let's start

Firstly there is lots of confusion about wheather linux is operating system or kernel and the 
correct answer of that is Kernel. 

What is actualy kernel?
     A software program which is designed to communicate with the hardware of the computer. It helps Operating System to      send signals or data do to particular task.
    It includes subsystems that performs taks as assigned.

    > The core susbsystems of Linux Kernel are:
       1. Process Schedular
       2. Memory Management Unit
       3. Virtual File System
       4. Networking Unit
       5. Inter-Process Communication Unit
This is all about kernel let's move forwa

Whenever we talk about linux two names arieses with it UNIX and GNU.

UNIX: It is operating system designed in 1969 which introdued some measure concepts which are usefull still now

    > file system hierarchy (/home, /etc)
    > multi-user system
    > multitasking
    > permissions
    > shell commands

Many systems are inspired by Unix:
   
    > Macos
    > Linux

GNU: Richard Stallman in 1983 comes with agenda to build open source operating system.
They have build:

    > bash shell
    > gcc compiler
    > core utilities (ls, cat, rm, etc.)
    > libraries
    > system tools

There kernel was not ready. They were facing issues while buliding kernel names Hurd.

Then the Linus Toravalds comes in picture in 1991 Linus publish his kernel with open source licence free for any one.
Because of open source stallman collaborated with Linux kernel and made GNU/Linux. Many Operating systems like Ubuntu and debian family is based on GNU/Linux.

Might question arises in your mind what is actualy Ubuntu and debian are? 

Let's understand:

As you know Linux is a kernel which is comunicator between hardaware and software(OS).
There are dozens of Operating System based on Linux kernel. Linux based operating systems are called linux flavours or Distro or Families.

![Linux Distro](images/image.png)

Three populer Distros are:
    
    > Debian: It is maintened by Canonical
    > Read Hat Enterprices Linux(RHEL): Maintened by RedHat.
    > Suse Linux Enterprices: Maintened by SUSE. 

These distros are built on the 3 factors:
    1. Liux kernel
    2. Package Management System: 
       > Debian: The file extension in debian is .deb and package manager is apt
       > Red Hat: Uses .rpm file extension and yum/dnf package manager.
       > SUSE: Uses .rpm file extension and zypper package manager.
    3. Init System:
       > Systemd
       > init
       > upstart(loader)

Each linux distribution built special Purpose:
        | ------ | ----------------------- | ---------------------- |
        | Debian | Stability + foundation  | Servers, base OS       |
        | Ubuntu | Ease + modern ecosystem | Cloud, DevOps, desktop |
        | RHEL   | Enterprise reliability  | Corporate production   |
        | SUSE   | Enterprise management   | SAP, enterprise infra  |

As you seen in the image Operating Systems that have been derived by distros.
 
      > Ubuntu -> Debian
      > Fedora -> RHEL
      > SUSE   -> SUSE Linux Enterprises 

As of now we have seen what is linux and linux based operating system. 


